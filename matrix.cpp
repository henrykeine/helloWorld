#include<stdio.h>
int main()
{   // henry kanagisa
	int i,j,k,l,first[10][10],second[0][10],sum[10][10];
	printf("enter no of row and column for first matrix ");
	scanf("%d%d",&k,&l);
	printf("enter no of row and column for second matrix");
	scanf("%d%d",&k,&l);
	printf("enter the elements of first matrix");
	for(i=0;i<k;i++)
	{
		for(j=0;j<l;j++)
		{
			
		}
	}
	scanf("%d",&first[i][j]);
	printf("enter the elements of second matrix");
	for(i=0;i<k;i++)
	{
		for(j=0;j<l;j++);
		{
			
		}
	}
	scanf("%d",&second[i][j]);
	sum[i][j]=first[i][j]+second[i][j];
	printf("sum of 2d matrix is");
	for(i=0;i<k;i++)
	{
		for(j=0;j<l;j++)
		{
			
		}
	}
	printf("%d",&sum[i][j]);
	}
